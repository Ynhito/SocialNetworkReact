let initialState = {
  friendsData: [
    { id: 1, name: 'Dima' },
    { id: 2, name: 'Julia' },
    { id: 3, name: 'Victor' }
  ]
};

const friendsReducer = (state = initialState, action) => {

  return state;
}

export default friendsReducer;